# Core dependencies for Gravitas-K
# RTX 5090D requires CUDA 12.8+ Nightly

# PyTorch (install with matching CUDA 12.8+ separately as needed)
torch>=2.4.0
torchvision>=0.19.0
torchaudio>=2.4.0

# Transformers and related
transformers>=4.51.0
accelerate>=0.26.0
safetensors>=0.4.0
tokenizers>=0.19.0

# Quantization and optimization
bitsandbytes>=0.42.0
peft>=0.7.0

# Attention optimization (optional, install separately if needed)
# flash-attn>=2.5.0  # Requires separate installation with: pip install flash-attn --no-build-isolation

# Vector search for PRC
faiss-gpu>=1.7.4

# Training utilities
wandb>=0.16.0
tensorboard>=2.15.0
tqdm>=4.66.0

# Data processing
datasets>=2.16.0
numpy>=1.24.0
pandas>=2.1.0
scipy>=1.11.0

# Configuration and utilities
pyyaml>=6.0
omegaconf>=2.3.0
rich>=13.7.0

# Testing
pytest>=7.4.0
pytest-cov>=4.1.0

# Code quality
black>=23.12.0
ruff>=0.1.0
isort>=5.13.0

# Optional: Inference servers (install separately if needed)
# vllm>=0.3.0
# sglang>=0.1.0

# Development tools
ipython>=8.18.0
jupyter>=1.0.0
gradio>=4.0.0  # For interactive debugging UI
streamlit>=1.29.0  # Alternative UI framework
